# internal-maximowood

Maximo Sales Calculator (Next.js). The application lives in `apps/web/` —
see `apps/web/README.md`. The Vercel project builds from that directory.

Previous platform state: branch `backup/producao-2026-08-20` / tag `producao-2026-08-20`.
